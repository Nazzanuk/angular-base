app.controller('AboutScreen', ($element, $timeout, $scope) => {

    var init = () => {
        //$timeout(() => $element.find('[screen]').addClass('active'), 50);
    };

    init();

    _.extend($scope, {

    });
});



